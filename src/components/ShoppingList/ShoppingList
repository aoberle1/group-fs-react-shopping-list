function ShoppingList (props) {


    return (

        <div>
            <button>Reset</button>
            <button>Clear</button>
            {props.groceryList.map(item => {
                <div id={item.id}>{item.item}{item.quantity}{item.unit}</div>
            }
            )}
        </div>
    )
}

export default ShoppingList;